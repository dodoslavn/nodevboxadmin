{
  "name": "nodevboxadmin",
  "version": "0.1.1",
  "private": true,
  "description": "Website for managing VirtualBox VMs on a single Debian host. No npm dependencies by design - see ARCHITECTURE.md.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "password-reset": "node config/password-reset.js"
  },
  "engines": {
    "node": ">=20"
  }
}
